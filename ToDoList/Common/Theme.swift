import UIKit

enum Theme {

    static let accent = UIColor(red: 0.996, green: 0.827, blue: 0.239, alpha: 1.0)

    static let background = UIColor.black

    static let primaryText = UIColor.white

    static let secondaryText = UIColor(white: 1.0, alpha: 0.5)

    static let completedText = UIColor(white: 1.0, alpha: 0.35)
}
